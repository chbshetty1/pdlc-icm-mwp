# Learnings

<!-- Copy to <product-repo>/LEARNINGS.md at product init, alongside
     .mwp/GLOBAL_CONTEXT.md (see README.md's "Using this framework for a
     new product" copy steps).

     Distinct from both GLOBAL_CONTEXT.md (deliberate, stable constraints —
     stack, brand rules, shared schemas) and LESSONS_LEARNED.md from entry
     0008 (why a whole feature was pivoted). This file is for incidental
     discoveries worth remembering that don't rise to either of those —
     a third-party API's undocumented rate limit, a footgun in a library,
     a quirk of the deployment environment.

     Append-only, one line per discovery, from any stage. Optional but
     recommended reading for 01_discovery_ideation, same pattern as
     LESSONS_LEARNED.md. If this file grows large and un-curated, that's
     itself a signal worth its own evolution entry (periodic curation or
     categorization) rather than something to solve upfront.
     See docs/evolution/0012-shared-learnings-file.md in the framework
     repo this product was built from. -->

| Date | Feature | Discovery |
|---|---|---|

<!-- template-version: 1 -->
