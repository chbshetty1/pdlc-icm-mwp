# Lessons Learned

<!-- Copy to <product-repo>/LESSONS_LEARNED.md at product init, same as
     GLOBAL_CONTEXT.template.md -> .mwp/GLOBAL_CONTEXT.md. Also self-created
     by scripts/pivot.sh on the first pivot if this copy step was skipped.

     Append-only register of pivoted (disproven) hypotheses -- read this
     before forming a new feature's riskiest assumption in
     01_discovery_ideation, so a similar idea that already failed isn't
     retested from scratch. Populated automatically by scripts/pivot.sh on
     every --pivot; never hand-edit a past row (same append-only spirit as
     docs/evolution/ in the framework repo this product was built from).
     See docs/evolution/0008-lessons-learned-register.md in that framework
     repo for why this exists. -->

| Feature | Assumption Tested | Why It Failed | Date | Archive Path |
|---|---|---|---|---|

<!-- template-version: 1 -->
