# This is a framework template repo — it should never accumulate live product data.
features/
sprints/
.archive/
.mwp/GLOBAL_CONTEXT.md
graphify-out/
*.repomix-output.*
.DS_Store
node_modules/
__pycache__/
*.pyc

# Personal Obsidian vault config if this folder is opened as (or contains) a vault —
# plugin settings and workspace layout are local preference, not shared framework state.
.obsidian/
